# NXTBus Driver App — points at the backend server.
# Use your PC's LAN IP so a phone on the same WiFi can reach it.
# (localhost only works when running the app in a browser on this PC)
EXPO_PUBLIC_API_URL=http://192.168.29.59:3000
